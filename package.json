{
  "private": true,
  "type": "module",
  "scripts": {
    "docs:dev": "npx vitepress dev docs --no-git",
    "docs:build": "npx vitepress build docs --no-git && npm run generate:sitemap && npm run verify:seo",
    "docs:preview": "npx vitepress preview docs",
    "start": "npx vitepress dev docs --no-git",
    "build": "npx vitepress build docs --no-git && npm run generate:sitemap && npm run verify:seo",
    "generate:sitemap": "node scripts/generate-sitemap.js",
    "verify:seo": "node scripts/verify-seo.js",
    "optimize:images": "node scripts/optimize-images.js",
    "setup": "node scripts/setup.js"
  },
  "devDependencies": {
    "@netlify/plugin-sitemap": "^0.8.1",
    "@types/node": "^22.14.1",
    "@vitejs/plugin-vue": "^5.0.3",
    "@vue/devtools-api": "^6.5.1",
    "@vueuse/core": "^10.7.1",
    "glob": "^10.3.10",
    "sharp": "^0.33.2",
    "sitemap": "^7.1.1",
    "vitepress": "^1.6.3",
    "vue": "^3.4.15"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
